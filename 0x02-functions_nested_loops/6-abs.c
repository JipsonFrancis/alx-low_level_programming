#include "main.h"
/**
 * _abs - find absolute value for number
 *
 * @n : the number to find abs val
 *
 *
 * Return: on postive integer
 */

int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	return (n);
}
